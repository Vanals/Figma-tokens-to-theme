version: 2.1

jobs:
  build: 
    docker:
      - image: circleci/node:14.15.1
    steps:
      - checkout
      - run: yarn install

workflows: 
  # TODO rename "package" with repo name
  build_and_deploy:
    when:
      and:
      - equal: [ main, << pipeline.git.branch >> ]
    jobs:
      - build
